%a{:href => '/'} All Thoughts

%form{:action => "/update/#{@thought.id}", :method => 'post'}
	%dl
		%dt Thought
		%dd
			%textarea{:name => 'thought_body'}= @thought.body

		%input{:type => 'submit', :value => 'Submit'}