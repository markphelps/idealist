%h2 Edit Thought
%form{:action => "/lists/#{@list.id}/thoughts/update/#{@thought.id}", :method => 'post', :class => 'form-stacked'}
  %textarea{:name => 'thought_body'}= @thought.body
  %input{:type => 'submit', :value => 'Submit', :class => 'btn primary'}
