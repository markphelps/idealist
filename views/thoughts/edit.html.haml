%a{:href => "/lists/#{@list.id}/thoughts"} All Thoughts

%form{:action => "/lists/#{@list.id}/thoughts/update/#{@thought.id}", :method => 'post'}
	%dl
		%dt Thought
		%dd
			%textarea{:name => 'thought_body'}= @thought.body

		%input{:type => 'submit', :value => 'Submit'}